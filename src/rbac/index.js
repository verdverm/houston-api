import { DEPLOYMENT_EDITOR, WORKSPACE_EDITOR } from "constants";

// Mapping of entityTypes to role.
export const serviceAccountRoleMappings = {
  DEPLOYMENT: DEPLOYMENT_EDITOR,
  WORKSPACE: WORKSPACE_EDITOR
};
