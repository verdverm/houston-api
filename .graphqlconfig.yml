projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:8871/v1
  database:
    schemaPath: src/lib/generated/schema/prisma.graphql
    extensions:
      prisma: database/prisma.yml
